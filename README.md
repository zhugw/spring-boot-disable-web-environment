# spring-boot-read-command-param

when command param value have space , it cannot be receive completely, e.g.
```
./target/spring-boot-read-command-param-0.0.1-SNAPSHOT.jar --foo="hello world"
hello
```
